var assert = require('assert');
var { sendSMS } = require('../src/send/sms');

describe('SMS', function() {
  describe('test', function() {
    it('should send an SMS', function(done) {
      done()
      // sendSMS("GMA Village", "+14151115555")
      //   .then((data) => {        
      //     console.log("data", data)
      //     done()
      //   }).catch((err) => {
      //     console.log(err)
      //     done(err)
      //   })
    })
  })
})